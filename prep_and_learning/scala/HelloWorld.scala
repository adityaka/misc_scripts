object HelloWorld {
    def main(args : Array[String]) {
        println("Hello World");
        val b = "Some String";
        val c = b.subString(3, 6).length();
        val arr = Array[Int](1,2,3,4,5);
        for (var i = 0; i < arr.length; i++){

        }
    }
}